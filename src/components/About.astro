<section class="about" id="about">
  <div class="container">
    <header class="about__header">
      <p class="about__eyebrow">Professional Focus</p>
      <h2 class="section-title">Building products that matter</h2>
      <p class="lead">
        I connect product strategy with hands-on engineering, helping teams ship dependable software with thoughtful AI features, clear roadmaps, and calm execution.
      </p>
    </header>
    <div class="about__grid">
      <article class="about__card glass">
        <div class="about__card-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 2L2 7l10 5 10-5-10-5z"/>
            <path d="M2 17l10 5 10-5"/>
            <path d="M2 12l10 5 10-5"/>
          </svg>
        </div>
        <h3>What I do</h3>
        <ul>
          <li>Translate fuzzy ideas into testable prototypes, then scale them into production systems</li>
          <li>Guide architecture, data strategy, and UX decisions for AI-powered web products</li>
          <li>Support teams with pragmatic delivery plans and high-trust collaboration</li>
        </ul>
      </article>
      <article class="about__card glass">
        <div class="about__card-icon about__card-icon--purple">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="16 18 22 12 16 6"/>
            <polyline points="8 6 2 12 8 18"/>
          </svg>
        </div>
        <h3>Expertise</h3>
        <ul>
          <li>TypeScript, Astro, React, and Flutter for modern front-end experiences</li>
          <li>Java, Kotlin, and cloud-native services for resilient backends</li>
          <li>LLM evaluation, prompt systems, and data workflows for applied AI</li>
        </ul>
      </article>
      <article class="about__card glass">
        <div class="about__card-icon about__card-icon--teal">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 22c5.523 0 10-4.477 10-10S17.523 2 12 2 2 6.477 2 12s4.477 10 10 10z"/>
            <path d="M12 6v6l4 2"/>
          </svg>
        </div>
        <h3>Values</h3>
        <ul>
          <li>Accessible, inclusive experiences with measurable impact</li>
          <li>Transparent collaboration and knowledge sharing</li>
          <li>Continuous learning through real-world experiments</li>
        </ul>
      </article>
    </div>
  </div>
</section>

<style>
  .about {
    padding-block: clamp(var(--space-16), 10vw, var(--space-24));
    position: relative;
  }

  .about__header {
    text-align: center;
    max-width: 720px;
    margin-inline: auto;
    margin-bottom: var(--space-16);
  }

  .about__eyebrow {
    font-size: var(--type-sm);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-primary);
    margin-bottom: var(--space-4);
  }

  .about__header .lead {
    margin-inline: auto;
  }

  .about__grid {
    display: grid;
    gap: var(--space-6);
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  }

  .about__card {
    padding: var(--space-8);
    border-radius: var(--radius-2xl);
    display: grid;
    gap: var(--space-5);
    align-content: start;
  }

  .about__card-icon {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-lg);
    background: linear-gradient(135deg, rgba(0, 113, 227, 0.1), rgba(41, 151, 255, 0.1));
    color: var(--color-primary);
  }

  .about__card-icon--purple {
    background: linear-gradient(135deg, rgba(88, 86, 214, 0.1), rgba(175, 82, 222, 0.1));
    color: #5856d6;
  }

  .about__card-icon--teal {
    background: linear-gradient(135deg, rgba(50, 215, 195, 0.1), rgba(52, 199, 89, 0.1));
    color: #34c759;
  }

  .about__card h3 {
    font-size: var(--type-xl);
    font-weight: 600;
    margin: 0;
  }

  .about__card ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: var(--space-3);
  }

  .about__card li {
    font-size: var(--type-base);
    color: var(--color-text-secondary);
    line-height: 1.6;
    padding-left: var(--space-6);
    position: relative;
  }

  .about__card li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.6em;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-border);
  }
</style>
